[uwsgi]
master = true
http = :5000
chdir = /projects/xhsweb/backend
wsgi-file = /projects/xhsweb/backend/run.py
callable = app
module = run:app
processes = 2
virtualenv = /envs/xhs/
route-uri = ^/socket\.io/socket\.io
route-type = rawfile
route-run = flask_socketio.SocketIO(app).manage_session()